def describepoint(x, y):
    match (x, y):
        case (0, y):
            return f"y-axis at y={y}"
        case (x, 0):
            return f"x-axis at x={x}"
        case _:
            return "Not on axis"

print(describepoint(3,0))